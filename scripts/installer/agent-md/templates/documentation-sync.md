## Documentation Sync

Before editing any file under `docs/wiki/` or a `README.md`, and whenever a change affects documented behavior (API, package, toolchain, structure), invoke the **Documentation Sync** skill and follow its procedure — it keeps those docs from contradicting the codebase.
